import 'package:flutter/cupertino.dart';

class EmployeesSection extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Employees Section'));
  }
}